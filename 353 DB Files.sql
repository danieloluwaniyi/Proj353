create table "PROJECT353".SUBMISSIONS
(
	USER_ID VARCHAR(15),
	SUBMISSION_ID BIGINT default 1 not null primary key,
	RATING DOUBLE,
	SUBMISSION_DATE DATE default CURRENT DATE,
	SUBMISSION_CONTENT BLOB(2147483647)
);
